# Application name
spring.application.name=Personal-Finance-Management

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://3.86.213.76:3306/personal-finance-management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=@naresh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Server port
server.port=8081

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Explicitly set Hibernate dialect for MySQL 8+
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Connection Pool Configuration (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=30000

# Optional Flyway configuration (uncomment if you want to use)
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration
